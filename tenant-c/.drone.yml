---
kind: pipeline
name: default
type: kubernetes

platform:
  os: linux
  arch: amd64

steps:
- name: echo
  pull: if-not-exists
  image: alpine
  commands:
  - echo tenant-c pipeline
  when:
    event:
    - push
