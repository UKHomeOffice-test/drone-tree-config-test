---
kind: pipeline
name: default
type: kubernetes

platform:
  os: linux
  arch: amd64

steps:
- name: echo-b
  pull: if-not-exists
  image: alpine
  commands:
  - echo tenant-b pipeline
  when:
    event:
    - push
