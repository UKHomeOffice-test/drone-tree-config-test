---
kind: pipeline
name: tenant-a
type: kubernetes

platform:
  os: linux
  arch: amd64

steps:
- name: echo
  pull: if-not-exists
  image: alpine
  commands:
  - echo tenant-a pipeline
  - sleep 10
  when:
    event:
    - push
